# ---------------------------------------------------------------------------
# Application
# ---------------------------------------------------------------------------
APP_NAME=Arvel
APP_ENV=development
APP_DEBUG=true
APP_KEY=
APP_URL=http://localhost:8000

# ---------------------------------------------------------------------------
# Database
# ---------------------------------------------------------------------------
# "sqlite" for local dev (no external service needed)
# docker compose overrides to pgsql via DB_URL
DB_DRIVER=sqlite
DB_DATABASE=database/database.sqlite

# pgsql/mysql (used when DB_DRIVER != sqlite)
DB_URL=
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USERNAME=arvel
DB_PASSWORD=

DB_ECHO=true
DB_POOL_SIZE=10
DB_POOL_MAX_OVERFLOW=5
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600
DB_POOL_PRE_PING=true
DB_EXPIRE_ON_COMMIT=false

# ---------------------------------------------------------------------------
# Logging / Observability
# ---------------------------------------------------------------------------
OBSERVABILITY_LOG_LEVEL=debug
OBSERVABILITY_LOG_FORMAT=auto
OBSERVABILITY_LOG_COLOR_MODE=auto
OBSERVABILITY_LOG_COLOR_DISABLE_IN_CI=true

# ---------------------------------------------------------------------------
# Session / Cache / Queue
# ---------------------------------------------------------------------------
SESSION_DRIVER=memory
SESSION_LIFETIME=120

# "memory" for local dev, "redis" when running docker compose
CACHE_DRIVER=memory
CACHE_PREFIX=
CACHE_DEFAULT_TTL=3600
CACHE_REDIS_URL=redis://localhost:6379/0

# "sync" for local dev, "redis" or "taskiq" when running docker compose
QUEUE_DRIVER=sync
QUEUE_DEFAULT=default
QUEUE_REDIS_URL=redis://localhost:6379
QUEUE_TASKIQ_BROKER=redis
QUEUE_TASKIQ_URL=redis://localhost:6379/1

# ---------------------------------------------------------------------------
# Mail
# ---------------------------------------------------------------------------
# "log" for local dev (prints to stdout), "smtp" when running docker compose
MAIL_DRIVER=log
MAIL_FROM_ADDRESS=noreply@localhost
MAIL_FROM_NAME=Arvel
MAIL_SMTP_HOST=localhost
MAIL_SMTP_PORT=1025
MAIL_SMTP_USERNAME=
MAIL_SMTP_PASSWORD=
MAIL_SMTP_USE_TLS=false
MAIL_TEMPLATE_DIR=templates/mail

# ---------------------------------------------------------------------------
# Storage (S3 / Local)
# ---------------------------------------------------------------------------
# "local" for local dev, "s3" when running docker compose (MinIO)
STORAGE_DRIVER=local
STORAGE_S3_BUCKET=arvel-dev
STORAGE_S3_REGION=us-east-1
STORAGE_S3_ENDPOINT_URL=http://localhost:9000
STORAGE_S3_ACCESS_KEY=minioadmin
STORAGE_S3_SECRET_KEY=minioadmin

# ---------------------------------------------------------------------------
# Lock
# ---------------------------------------------------------------------------
LOCK_DRIVER=memory
LOCK_DEFAULT_TTL=30
LOCK_REDIS_URL=redis://localhost:6379/0

# ---------------------------------------------------------------------------
# Media  (disk is inherited from STORAGE_DRIVER above)
#   conversion_quality: JPEG/WEBP quality 1-100 (default 85)
#   conversion_format:  "source" keeps original format, or "webp"/"jpeg"/"png"
# ---------------------------------------------------------------------------
MEDIA_MAX_DIMENSION=10000
MEDIA_PATH_PREFIX=media
MEDIA_CONVERSION_QUALITY=85
MEDIA_CONVERSION_FORMAT=source

# ---------------------------------------------------------------------------
# Notifications
# ---------------------------------------------------------------------------
NOTIFICATION_DEFAULT_CHANNELS=["mail"]
NOTIFICATION_SLACK_WEBHOOK_URL=
NOTIFICATION_DATABASE_TABLE=notifications

# ---------------------------------------------------------------------------
# Scheduler
# ---------------------------------------------------------------------------
SCHEDULER_ENABLED=true
SCHEDULER_TIMEZONE=UTC
SCHEDULER_LOCK_BACKEND=memory

# ---------------------------------------------------------------------------
# Search
# ---------------------------------------------------------------------------
# "collection" = in-memory (no external service needed, good for dev/test)
# "meilisearch" = Meilisearch (docker compose overrides to this)
SEARCH_DRIVER=collection
SEARCH_PREFIX=
SEARCH_QUEUE_SYNC=false
SEARCH_MEILISEARCH_URL=http://localhost:7700
SEARCH_MEILISEARCH_KEY=

# ---------------------------------------------------------------------------
# Broadcasting  (Valkey/Redis pub/sub via docker compose)
# ---------------------------------------------------------------------------
# "memory" = in-process (no Redis needed)
# "redis"  = Valkey/Redis pub/sub (production / docker compose)
# "log"    = structlog output (debugging)
# "null"   = discard all broadcasts
BROADCAST_DRIVER=memory
BROADCAST_REDIS_URL=redis://localhost:6379/2
BROADCAST_AUTH_ENDPOINT=/broadcasting/auth
